#include <stdio.h>
#include <math.h>

void main()
{
		int sum;
		int last2[2];
		
		sum = 0
		last3 = { 01, 2, 4 };

		while( sum <= 4000000)
		{

			last3[0] = last1 * last2 };
			last3[1] = last1 * 2 };
			last3[2] = last2 * 2 };

		if( )

		}
		
		while( mult5 <= 999 )
		{
			if( mult5 % 3 != 0 )
			{
				total = total + mult5;
			}
			mult5 = mult5 + 5;
		}
		
		printf ("TOTAL = %d", total);
}